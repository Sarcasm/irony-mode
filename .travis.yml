language: cpp

compiler:
  - gcc
  - clang

before_install:
  - git submodule update --init --recursive
  - sudo apt-get install emacs libclang-dev -qq

before_script:
  - mkdir build
  - cd build

script:
  - cmake ../server
  - make -k
  - ctest --output-on-failure 

after_script:
  - cd -
